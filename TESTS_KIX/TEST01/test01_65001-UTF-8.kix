Call LUDecode.kxl

$Dir = "c:\windows"
$Dir = @CurDir
$s = "Список файлов"

;SHELL "CMD.EXE chcp 65001"

;RUN "CMD.EXE chcp 65001"

? 
"-------------------------"
?
$s

;? "------UTF8toWin($s)-------------------"
;$s = UTF8toWin($s)
;?
;$s

? "-------------------------"
$FSO = CreateObject("Scripting.FileSystemObject")
if $FSO
   $Folders = $FSO.GetFolder($Dir)
   $Files = $Folders.Files
   for Each $File in $Files
      ? $File.name 
   Next
endif 
