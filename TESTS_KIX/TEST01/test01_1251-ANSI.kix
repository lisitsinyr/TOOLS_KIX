Call LUDecode.kxl

$Dir = "c:\windows"
$Dir = @CurDir
$s = "Список файлов"

? 
"-------------------------"
?
$s

;? "------UTF8toWin($s)-------------------"
;$s = UTF8toWin($s)
;?
;$s

? "-------------------------"
$FSO = CreateObject("Scripting.FileSystemObject")
if $FSO
   $Folders = $FSO.GetFolder($Dir)
   $Files = $Folders.Files
   for Each $File in $Files
      ? $File.name 
   Next
endif 
